import React, { useMemo, useState } from 'react';
import { Clock3, PlusCircle } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from '@/components/ui/dialog';
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';
import { DataTable, PageHeader, SearchBar } from '@/tenant/components/TenantUI';
import { AttendanceForm } from '@/tenant/hr/HrForms';
import { HrStatusBadge } from '@/tenant/hr/HrStatusBadge';
import { useHrData } from '@/tenant/hr/HrDataProvider';
import { HR_DEMO_TODAY } from '@/tenant/hr/hrDemoService';

const AttendancePage: React.FC = () => {
  const hr = useHrData();
  const [query, setQuery] = useState('');
  const [formOpen, setFormOpen] = useState(false);
  const todayEntries = useMemo(() => hr.attendance.filter((entry) => entry.date === HR_DEMO_TODAY && entry.employeeName.toLowerCase().includes(query.toLowerCase())), [hr.attendance, query]);
  const monthDays = Array.from({ length: 18 }, (_, index) => index + 1);

  return <div><PageHeader title="Attendance" description="Daily, monthly, and employee-level attendance with work hours and location context." action={<Button onClick={() => setFormOpen(true)}><PlusCircle className="h-4 w-4" />Mark attendance</Button>} /><Tabs defaultValue="daily"><TabsList className="max-w-full justify-start overflow-x-auto"><TabsTrigger value="daily">Daily attendance</TabsTrigger><TabsTrigger value="monthly">Monthly attendance</TabsTrigger><TabsTrigger value="employee">Employee-wise</TabsTrigger></TabsList><TabsContent value="daily" className="mt-4"><div className="mb-4 max-w-md"><SearchBar value={query} onChange={setQuery} placeholder="Search employees" /></div><DataTable headers={['Employee', 'Date', 'Check-in', 'Check-out', 'Status', 'Work hours', 'Location']}>{todayEntries.map((entry) => <tr key={entry.id}><td className="px-4 py-3 font-medium text-slate-950">{entry.employeeName}</td><td className="px-4 py-3 text-slate-600">{entry.date}</td><td className="px-4 py-3 text-slate-600">{entry.checkIn || '-'}</td><td className="px-4 py-3 text-slate-600">{entry.checkOut || '-'}</td><td className="px-4 py-3"><HrStatusBadge status={entry.status} /></td><td className="px-4 py-3 text-slate-600">{entry.workHours || 0} hrs</td><td className="px-4 py-3 text-slate-600">{entry.location || 'Not recorded'}</td></tr>)}</DataTable></TabsContent><TabsContent value="monthly" className="mt-4"><DataTable headers={['Employee', ...monthDays.map(String)]}>{hr.employees.map((employee) => <tr key={employee.id}><td className="sticky left-0 bg-white px-4 py-3 font-medium text-slate-950">{employee.name}</td>{monthDays.map((day) => { const date = `2026-06-${String(day).padStart(2, '0')}`; const entry = hr.attendance.find((item) => item.employeeId === employee.id && item.date === date); const code = entry?.status === 'Present' ? 'P' : entry?.status === 'Absent' ? 'A' : entry?.status === 'Late' ? 'L' : entry?.status === 'Half Day' ? 'H' : entry?.status === 'Leave' ? 'LV' : '-'; return <td key={day} className={`px-2 py-3 text-center text-xs font-semibold ${code === 'A' ? 'text-red-600' : code === 'P' ? 'text-emerald-700' : 'text-amber-700'}`}>{code}</td>; })}</tr>)}</DataTable></TabsContent><TabsContent value="employee" className="mt-4 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{hr.employees.map((employee) => { const entries = hr.attendance.filter((entry) => entry.employeeId === employee.id && entry.date.startsWith('2026-06')); const present = entries.filter((entry) => ['Present', 'Late', 'Half Day'].includes(entry.status)).length; const hours = entries.reduce((sum, entry) => sum + entry.workHours, 0); return <article key={employee.id} className="rounded-md border border-slate-200 bg-white p-4 shadow-sm"><div className="flex items-start justify-between gap-3"><div><h2 className="font-semibold text-slate-950">{employee.name}</h2><p className="mt-1 text-xs text-slate-500">{employee.department}</p></div><Clock3 className="h-4 w-4 text-indigo-600" /></div><div className="mt-4 grid grid-cols-2 gap-3"><Metric label="Present days" value={String(present)} /><Metric label="Work hours" value={hours.toFixed(1)} /></div></article>; })}</TabsContent></Tabs><Dialog open={formOpen} onOpenChange={setFormOpen}><DialogContent className="sm:max-w-2xl"><DialogHeader><DialogTitle>Mark attendance</DialogTitle><DialogDescription>Create or update an employee attendance entry.</DialogDescription></DialogHeader><AttendanceForm employees={hr.employees} onSubmit={(draft) => { hr.markAttendance(draft); setFormOpen(false); }} /></DialogContent></Dialog></div>;
};
const Metric: React.FC<{ label: string; value: string }> = ({ label, value }) => <div className="rounded-sm bg-slate-50 p-3"><p className="text-xs text-slate-500">{label}</p><p className="mt-1 text-lg font-semibold text-slate-950">{value}</p></div>;
export default AttendancePage;
